import styled from 'styled-components';

export const StListWrapper = styled.div`
    display: flex;
    flex-direction: column;
    flex: 1;
    width: 100%;
    border-radius: 10px;
    background-color: var(--second-color);
    overflow-y: auto;
`;
export const StUlist = styled.ul`
    overflow-x: hidden;
    overflow-y: auto;
`;
